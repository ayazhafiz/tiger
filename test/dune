(tests
  (names flow_live_test runtime_test)
  (modules Flow_live_test Runtime_test)
  (libraries Tiger alcotest)
  (deps (source_tree ../runtime)))

(executable
  (name compiler)
  (modules Compiler)
  (libraries Tiger alcotest))
